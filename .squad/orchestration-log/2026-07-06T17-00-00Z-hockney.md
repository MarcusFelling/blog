# Orchestration Log — Hockney

**Timestamp:** 2026-07-06T17:00:00Z
**Agent:** Hockney (Tester)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (tests/ + skill only; no site source changed)

## Why Routed

Pre-redesign regression baseline. Tester establishes a green, measurable starting point and the selector contract so that once the restyle lands, any red is provably a redesign regression and the must-not-change chrome (nav, footer) can be visually diffed.

## Files Read / Modified

- All 12 Playwright specs under `tests/`
- `tests/images.spec.ts` — **fixed** one pre-existing content-coupled red (see Outcome)
- `tests/baseline/` — added read-only baseline-capture tooling + screenshots (gitignored PNGs)
- `.squad/skills/jekyll-workflow/SKILL.md` — refreshed the stale "Test Suite Coverage" table (6 → 12 specs), confidence low → medium

## Files Produced

- `.squad/decisions/inbox/hockney-redesign-baseline.md` — baseline result + the full selector contract

## Outcome

**Baseline: 65/65 GREEN** (chromium, ~28s, against a frozen `_site_baseline` build). One pre-existing red was found and fixed **first**: `images.spec.ts` "images load successfully" assumed every one of the latest 10 posts has an image, but the 2026-06-16 vibe-analytics post is text-only — a content coupling, not a redesign regression (fixed per the existing "remove content-coupled tests" decision; also fixed a `page.on('response')` listener leak). Enumerated the **~80-hook selector contract** (the redesign's API) grouped by spec — rename only with a matching spec update in the same PR. Reusable methodology captured: build a frozen snapshot (`-d _site_baseline`, fresh destination to sidestep the `incremental:true` staleness gotcha) and serve it `--no-watch` so the suite never races McManus's live edits; Jekyll's own server (not `python -m http.server`) is required for the real-404 test. Baseline screenshots (homepage desktop+mobile, navbar, footer, post-with-code) staged for later visual diff.
