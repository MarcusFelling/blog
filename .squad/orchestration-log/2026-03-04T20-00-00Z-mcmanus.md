# Orchestration Log — McManus (Frontend Dev)

**Session:** 2026-03-04T20-00-00Z  
**Agent:** McManus  
**Role:** Frontend Dev

## What Was Done

1. **Recreated `_layouts/post.html`** — File had been deleted. Rebuilt from scratch using `layout: base`. Includes:
   - Post header with title, subtitle, and date meta
   - Post body and thumbnail rendering
   - Tag list with inline slug normalization matching `archives.md` filter keys
   - Prev/next pagination

2. **Tag slug normalization in `_layouts/post.html`** — Added `if/elsif` chain mirroring the one in `archives.md`:
   - `Azure Pipelines` → `azure-devops`
   - `Azure DevOps` → `azure-devops`
   - `GitHub Copilot` → `ai`
   - `DevOps` → `cicd`
   - `CICD` → `cicd`
   - Fallback: `tag | slugify`

3. **`activateFromHash()` JS added to `archives.md`** — On page load (and on `hashchange`), the archives JS reads `window.location.hash`, strips the `#`, and clicks the matching `data-filter` button. Enables deep-linking to tag sections from post tag links.

## Decisions Dropped to Inbox

- `mcmanus-tag-routing-fix.md` — normalization sync rule between `post.html` and `archives.md`.
- `mcmanus-highlight-fix.md` — CSS fix for empty space in Jekyll code blocks.

## Files Modified

- `_layouts/post.html` (recreated)
- `archives.md`
- `assets/css/blog.css`
