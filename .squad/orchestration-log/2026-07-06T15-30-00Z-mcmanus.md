# Orchestration Log — McManus

**Timestamp:** 2026-07-06T15:30:00Z
**Agent:** McManus (Frontend Dev)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (staged, not committed)

## Why Routed

M1 — the `extract` gate of the impeccable redesign. Frontend Dev owns CSS/layout/JS. Nothing else in the sequence starts until the design tokens have a single source of truth.

## Files Read

- `assets/css/blog.css`, `_includes/head.html`, `_layouts/base.html`, `_layouts/home.html`
- Grepped CSS/HTML/JS for every `var()` consumer before dropping any token

## Files Produced / Modified

- `assets/css/blog.css` — merged the second `:root` into section 1; moved the homepage/hero/card/footer chrome into a new tail section 15; fixed the body-font bug
- `_includes/head.html` — reduced to metadata / shared-head only (no inline `<style>`, no `:root`)
- `.squad/decisions/inbox/mcmanus-m1-token-consolidation.md` — the M1 decision

## Outcome

The blog now has **one** token system, in `assets/css/blog.css`; `_includes/head.html` is metadata-only (never add a `:root` or inline `<style>` there again). Behavior-preserving refactor with **one deliberate fix**: `body.modern` no longer forces the never-loaded Open Sans — body copy is now Inter (`var(--body-font)`), verified in-browser (computed `font-family` flips from `"Open Sans", system-ui…` to `Inter…`). Dropped 10 provably-dead tokens (0 `var()` refs); kept `--accent-grad`, the `sheen` veil, radial background, and glass `backdrop-filter`s untouched (removing them would change the render — those are M2 `distill`/`quieter` targets). Cascade-safe technique: appended the moved rules at the end of `blog.css` to preserve the ties the inline block used to win. Every Playwright selector preserved verbatim. Build clean (`-d _site_check`, deleted); committed `_site/` untouched so Hockney's baseline stays intact. **Staged, not committed.**
