# Orchestration Log — McManus

**Timestamp:** 2026-07-06T20:00:00Z
**Agent:** McManus (Frontend Dev)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (staged, not committed)

## Why Routed

The optional polish pass — clear the three non-blocking findings Hockney handed back from the editorial-restyle regression. Frontend Dev owns the CSS/include edits; additive only, personality unchanged (type/layout/accent, no new effects).

## Files Read

- `.squad/decisions/inbox/hockney-redesign-regression.md` (the three findings), `.squad/decisions.md` (Blog Redesign section — follow-ups + guardrails)

## Files Modified

- `assets/css/blog.css` — (a) branded `:focus-visible` ring on `.search-trigger` + `.cmd-palette-hint` (`outline: 2px solid var(--focus-ring); outline-offset: 2px`, matching the `.nav-links a:focus-visible` convention); (b) `overflow-wrap: anywhere` on `.blog-post a` + zeroed the un-carded `.post-body > .row` (and `.header-section .container-md > .row`) margins to kill the Bootstrap negative-gutter spill
- `_includes/footer-scripts.html` — removed the dead `DOMContentLoaded` handler (inert `[data-delay]` loop + `.read-more-chip` click-shim), replaced with a Liquid comment placeholder so `base.html`'s `{% include %}` stays valid
- `.squad/decisions/inbox/mcmanus-polish-pass.md` — the polish delta + the out-of-scope pre-existing-overflow triage note

## Outcome

Three findings cleared across two files, additive. **(1)** Focus ring: confirmed `:focus-visible = true` + computed outline `rgb(249,115,22)` on both controls under real keyboard **Tab** (programmatic `.focus()` doesn't engage `:focus-visible` in Chromium). **(2)** Long-URL overflow: the `.blog-post a` wrap rule is load-bearing — A/B toggling it off at 320px overflows on 23 posts, on it 0; row margins now compute `0px/0px` with no card chrome reintroduced. **(3)** Dead JS gone; homepage entrance still animates via CSS (`rise`, staggered, opacity 1), zero console errors. Verified on a 54-post 320px crawl over a scratch `_site_check` build (deleted after); `_site/` + `_site_baseline` untouched. **Staged, not committed.** Flagged the remainder of the 320px scroll as pre-existing content (his draft hypothesized the off-canvas `.mobile-drawer` as the dominant driver) for Hockney to re-verify.
