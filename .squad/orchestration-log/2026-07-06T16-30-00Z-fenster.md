# Orchestration Log — Fenster

**Timestamp:** 2026-07-06T16:30:00Z
**Agent:** Fenster (Design/UX)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-only (design spec; no site files changed)

## Why Routed

With Marcus's direction fixed (distinctive editorial, dark-only), Fenster produced the build-ready design spec — the exact typographic, layout, color, motion, and a11y intent McManus implements and Hockney gates against.

## Files Read

- `assets/css/blog.css` (post-M1), `_layouts/base.html`, `_data/top_pages.yml`
- `.squad/decisions/inbox/coordinator-redesign-direction.md`, `keaton-redesign-scope.md`, `hockney-redesign-baseline.md`

## Files Produced

- `.squad/decisions/inbox/fenster-editorial-spec.md` — the authoritative design spec (**supersedes** `fenster-redesign-direction.md`)
- `.squad/skills/editorial-dark-theme/SKILL.md` — new skill

## Outcome

Full "distinctive editorial" spec. **Type:** **Fraunces** (variable serif) as the display "title voice" **only** — hero headline, post `h1`, featured card title; everything else Inter, code JetBrains Mono. Chose Fraunces over the AI-defaults Playfair Display / Space Grotesk. **One heading treatment** — collapse the h2-underline+gradient / h3-left-bar / h4-italic mess into a single solid orange kicker rule on `h1` + `h2`. **Layout:** asymmetric left-weighted hero (~62/38) with the Popular list in the right negative space; post grid = featured (Fraunces, full-width) + Inter index (3→2→1 col); un-nest the post card-on-card by un-carding `.container-md` (keep Bootstrap cols) at a 72ch measure. **Color:** `#f97316` as the only accent; kill blue/`--accent-grad`/glass/sheen/`pulse-glow`; blur reserved for command palette + drawer; per-tag palette kept but scoped to archives + related chips. **A11y:** AA-verified muted-text floor `#8b949e`. Includes a compact `:root` token table for direct implementation. Enumerated the selector-contract impact for Hockney (removed `.hero-panel`/`.hero-kpis`/`.read-more-chip`; new `.popular-posts`/`.popular-post-link`/`.post-card--featured`).
