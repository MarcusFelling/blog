# Orchestration Log — Hockney

**Timestamp:** 2026-07-06T20:30:00Z
**Agent:** Hockney (Tester)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (tests/ only; no site source changed)

## Why Routed

Polish regression — verify McManus's three fixes against a fresh frozen build and quantify/attribute the pre-existing 320px overflow he flagged. Tester owns the paired test-contract update; per Keaton's rule the added assertion lands with the change it guards.

## Files Read / Modified

- `tests/landing.spec.ts` — added one non-flaky focus-ring test (Tab-*searches* to each ⌘K control, no hard-coded Tab count so nav reordering won't flake it); suite 70 → 71
- `tests/redesign/verify-polish.mjs` — new standalone harness (focus-ring Tab checks, long-URL wrap, console cleanliness, drawer `canScrollRight`/hide-to-attribute crawl); not collected by `npx playwright test`
- `.squad/skills/jekyll-workflow/SKILL.md` — refreshed the stale spec-count table

## Files Produced

- `.squad/decisions/inbox/hockney-polish-regression.md` — regression result + the corrected drawer attribution + the content-overflow follow-up

## Outcome

**12 specs / 71 tests GREEN** (chromium, ~29s, frozen `_site_redesign2` build, deleted after; validated `--repeat-each=3`, 27/27). All three fixes verified PASS: keyboard-Tab focus rings compute `2px solid rgb(249,115,22)`; `.blog-post a` wraps with 0 offenders at 320px on the two bare-autolink posts; dead-JS removal leaves 0 console/pageerror and CSS entrance intact. **Corrected McManus's drawer hypothesis:** an isolation measurement shows the `.mobile-drawer` contributes **0px** on all 48 posts (`position:fixed`, out of document flow, no transformed ancestor above it) — it's a red herring. The real ~30–360px horizontal overflow is **pre-existing content on 9/48 posts** (long unbroken `<em>`/`<code>` tokens, one unconstrained `<img>`, a wide code `<table>`) — a Fenster design call, NOT a regression and NOT the drawer. The standing "no mobile-drawer assertions until stable in CI" decision holds (reinforced). Screenshots skipped (the polish is invisible in every existing frame — rings need keyboard focus, wrap only shows at 320px). **Staged for Marcus's review; no commit.**
