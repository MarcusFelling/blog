# Orchestration Log — McManus

**Timestamp:** 2026-07-06T18:00:00Z
**Agent:** McManus (Frontend Dev)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (staged, not committed)

## Why Routed

M2 — the editorial restyle. Frontend Dev implements Fenster's build-ready spec on top of the M1-consolidated token system.

## Files Read

- `.squad/decisions/inbox/fenster-editorial-spec.md` (spec), `hockney-redesign-baseline.md` (§2/§7 contract)

## Files Modified

- `assets/css/blog.css` — full restyle on the single token source (Fenster's `:root` table, one heading kicker, asymmetric hero, featured+index grid, un-carded post, flat nav/footer, removed sheen/glass/gradient/pulse-glow)
- `_layouts/base.html` — added Fraunces to the single combined Google Fonts request
- `_layouts/home.html` — asymmetric hero, featured lead, `.popular-posts` list (Pulse/KPI/read-more-chip removed)
- `_layouts/post.html` — added `.post-body` class to un-card the structural container (Bootstrap grid kept)
- `.squad/decisions/inbox/mcmanus-editorial-restyle.md` — the selector delta + font change; updated `.squad/skills/editorial-dark-theme/SKILL.md`

## Outcome

Fenster's spec implemented: **Fraunces** on the three title slots (hero `h1`, post `h1`, featured card title) with Inter body; one solid orange kicker rule replacing the three old heading decorations; asymmetric left-weighted hero (≈61/39) with the quiet Popular list in the right column; featured (full-width Fraunces) + Inter index grid (3→2→1); post un-carded at 72ch with terminal code blocks intact; flat nav/footer (blur kept only on command palette + drawer); one orchestrated staggered load, reduced-motion safe (base opacity 1). Removed `--accent-grad` + its consumers, the `sheen` veil, radial background, and `pulse-glow`. Two gotchas encoded into the CSS + skill: **featured-card specificity** (`.post-card.modern-card.post-card--featured X` to outrank the base rules) and **`<nav>` picking up generic chrome** (switched the Popular list to `<aside>`). Verified in-browser (computed styles + desktop/tablet/mobile screenshots). **Staged, not committed** — the removed `landing.spec.ts` hooks are Hockney's paired update.
