# Orchestration Log — Hockney

**Timestamp:** 2026-07-06T19:00:00Z
**Agent:** Hockney (Tester)
**Requested by:** Marcus Felling
**Mode:** background
**Access:** read-write (tests/ only; no site source changed)

## Why Routed

Restyle regression + the paired test-contract update. Per Keaton's rule, McManus's removed/added selectors land with the matching `landing.spec.ts` change in the same PR, asserting placement rather than hard-coded lists.

## Files Read / Modified

- `tests/landing.spec.ts` — dropped the Pulse/KPI + `.hero-panel` assertions; added asymmetric-hero, featured-lead, and Popular-list placement assertions (4 → 8 tests)
- `tests/reading-time.spec.ts` — added a font guard (1 → 2 tests)
- Font guards on existing specs — computed `font-family`: Fraunces on the three title slots, Inter on body
- `tests/redesign/` — "after" screenshots + `verify-redesign.mjs` (gitignored PNGs; scripts tracked)

## Files Produced

- `.squad/decisions/inbox/hockney-redesign-regression.md` — regression result, before/after, a11y sweep, findings

## Outcome

**12 specs / 70 tests GREEN** (chromium, ~30s, frozen `_site_redesign` build). Suite grew 65 → 70 (landing 4→8, reading-time 1→2); only `tests/` was touched. Verified the paths McManus's narrow browser couldn't: **>1024 index grid = 3 columns**; `prefers-reduced-motion` kills the entrance (opacity 1, 0 infinite loops); AA contrast on all new muted text; `footer .social-chip` = 0. Font guard is network-independent (`getComputedStyle().fontFamily` returns the declared stack regardless of webfont fetch). Handed back **three non-blocking findings** to McManus: (1) `.search-trigger` + `.cmd-palette-hint` miss the branded `:focus-visible` ring; (2) the 320px post-page horizontal scroll is **pre-existing, not a regression** — A/B showed the restyle *halved* it (62px → 30px; driver is a long unwrapped URL in prose); (3) dead JS in `_includes/footer-scripts.html`. Verdict: **ship-ready from QA** — staged for Marcus's review.
